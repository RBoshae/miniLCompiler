function main;
beginparams
endparams

beginlocals

  n : integer;
  i, j, k : integer;
  x, y, z : array [20] of integer;

endlocals

beginbody	## main program
  read a;
  read i[0];
  read aa, bb, cc;
  read ii, jj[10], kk;
  ## write c;
  ## read b[10];   ## Can't hand this case without Expression
  i := 2 * 3 / 4 % 5;
  j := 15 / 5;
  k := 20 / 10;
endbody
